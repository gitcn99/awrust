# awrust

Rust 工作空间，包含 `cc-core` 核心公共库。

## 项目结构

```
awrust/
├── crates/
│   └── cc-core/          # 核心公共库
│       ├── src/          # 库源码
│       └── examples/     # 使用示例
├── config/               # 配置文件
└── Cargo.toml            # 工作空间配置
```
